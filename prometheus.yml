global:
  scrape_interval: 5s # Har 5 second me data khinchega

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080'] # Windows/Mac local machine par running app ke liye